# Private notes and instructions — never committed
CLAUDE.local.md
content/
drift-sentinel-build-plan.MD

# Machine-specific values (AIRFLOW_UID etc.)
.env

# Raw dataset and derived splits — versioned by checked-in hash, not by committing the bytes
data/

# Airflow runtime output
logs/

# Python
__pycache__/
*.py[cod]
.venv/
venv/
.pytest_cache/
